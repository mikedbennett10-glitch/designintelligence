# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic (server-side only — never expose to the client)
ANTHROPIC_API_KEY=

# Procore (server-side only — never expose to the client)
PROCORE_CLIENT_ID=
PROCORE_CLIENT_SECRET=
PROCORE_BASE_URL=https://api.procore.com

# GCP
GCP_PROJECT_ID=
GCS_BUCKET_ASSETS=

# Auth
NEXTAUTH_SECRET=
